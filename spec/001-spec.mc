# 知识库 AI 系统设计需求与设计文档

## 1. 项目概述

- **项目名称**：aichat 知识库 AI 系统
- **前端技术栈**：Vue3 + Vite（参考 `aichat/原型图/src` 的交互与布局风格）
- **后端技术栈**：Spring Boot（Java 11 / 17，使用 Spring Web、Spring Security、Spring Data JPA）
- **数据库**：H2 内存/文件数据库（开发环境），后续可平滑切换 MySQL
- **大模型服务**：智谱 AI API（如 GLM 系列），通过后端统一封装调用
- **整体架构**：前后端分离，REST API 通信，前端通过 Axios 调用后端接口

系统目标：
- 提供一个面向企业内部的「知识库 + AI 问答」系统。
- 用户可以登录系统，普通用户进入「AI 聊天/问答」页面，基于企业知识库进行智能问答。
- 管理员登录后默认进入「用户管理」页面，可管理用户角色（管理员/普通用户）与基础信息。

> 说明：前端界面交互和动效风格参考 `aichat/原型图/src` 目录中的现有实现和组件风格（如卡片、按钮、对话框、浅色主题），这里主要定义业务需求与接口设计。

---

## 2. 角色与权限

系统有两类核心角色：

1. **管理员（ADMIN）**
   - 拥有后台管理权限：
     - 管理用户（增删改查、设置角色）。
     - 后续可扩展：管理知识库数据源、模型参数等。
   - 登录后默认跳转到「用户管理页面」。

2. **普通用户（USER）**
   - 登录后默认进入「AI 聊天页面」，可：
     - 基于知识库进行提问。
     - 查看会话记录。
     - 管理个人基本信息（在后续版本可扩展）。

未登录用户只能访问登录页，不能访问其他页面。

---

## 3. 功能模块概述

### 3.1 登录与认证模块

**功能目标**：提供账号密码登录，区分管理员和普通用户，后续可扩展为 SSO / 第三方登录。

- 用户输入邮箱/账号 + 密码登录。
- 登录成功：
  - 若角色为 ADMIN → 跳转「用户管理页面」。
  - 若角色为 USER → 跳转「AI 聊天页面」。
- 支持「记住我」选项，在前端本地存储 token 或刷新 token。
- 失败场景：
  - 账号或密码错误，提示「账号或密码错误」。
  - 账号被禁用（后续可扩展），提示「账号已禁用，请联系管理员」。

### 3.2 用户管理模块（管理员可见）

**功能目标**：管理员可以维护系统用户列表与角色。

核心能力：
- 查看用户列表（分页）：
  - 字段：头像（可选）、姓名/昵称、邮箱、角色（管理员/用户）、状态（启用/禁用）、创建时间、最近登录时间。
- 新建用户：
  - 支持管理员创建账号，设置初始密码与角色。
- 编辑用户：
  - 修改姓名/昵称、邮箱、角色、状态。
- 重置密码：
  - 一键重置为系统默认密码或随机密码，并提示管理员结果（发送邮件可后续扩展）。

### 3.3 AI 聊天模块（普通用户默认入口，管理员也可访问）

**功能目标**：提供类似 ChatGPT 的对话界面，但回答尽量基于企业知识库内容进行。

核心能力：
- 左侧或顶部显示「会话列表」：
  - 支持新建会话、切换会话、删除会话。
- 主区域为对话内容流：
  - 用户消息与 AI 消息交替展示，采用气泡式 UI。
  - 支持代码块高亮、Markdown 基本渲染（可后续补充）。
- 底部输入区：
  - 支持多行输入、回车发送 / Ctrl+Enter 换行。
  - 支持「正在生成」状态展示与停止生成按钮。
- 基于知识库的约束：
  - 后端在调用智谱 API 前，对用户问题结合知识检索结果传给大模型（知识增强型 RAG 模式）。本期文档先定义接口与流程，具体检索实现可后续迭代。

### 3.4 知识库基础模块（本期只定义接口骨架）

本期重点是「AI 聊天 + 用户管理」，知识库本身可以先简化为：

- 表结构：
  - `kb_document`：id、标题、内容、标签、更新时间等。
  - 后端提供简单的「按关键词检索文档」接口。
- 在聊天模块中，后端根据用户问题调用该接口，检索若干文档摘要传入智谱模型的 prompt。

---

## 4. 前端设计（Vue3）

### 4.1 路由结构

- `/login`：登录页
- `/admin/users`：用户管理页面（仅管理员可访问）
- `/chat`：AI 聊天页面（登录用户均可访问）
- `/`：根路径
  - 登录后根据角色重定向到 `/admin/users` 或 `/chat`

### 4.2 页面说明

#### 4.2.1 登录页（`/login`）

参考 `zhishiku` 登录样式和截图：

- 布局：
  - 左上角品牌标识（如「Figma」或系统 Logo）。
  - 中间居中卡片：「知识库」标题，下方为登录表单。
- 表单元素：
  - 输入框 1：邮箱输入
  - 输入框 2：密码
  - 复选框：「记住我」
  - 链接：「忘记密码」（可先不实现真正逻辑）
  - 按钮：
    - 主按钮「登录」
    - 次要按钮「企业 SSO」「第三方登录」（仅 UI 占位）
- 交互：
  - 校验通过则调用后端 `/api/auth/login`。
  - 登录成功后根据返回的 `role` 字段做路由跳转。
  - 登录失败展示错误提示。

#### 4.2.2 用户管理页（`/admin/users`）

总体布局类似之前实现的「成员管理」页面：

- 顶部标题：成员列表 / 用户管理。
- 表格展示：
  - 列：头像、姓名、邮箱、角色、状态、最近登录、操作。
  - 操作列支持「编辑」「重置密码」等按钮。
- 邀请/创建用户区域：
  - 邮箱输入框 + 角色选择框 + 「创建/邀请」按钮。
  - 当前阶段仅调用后端创建用户接口，不做真正邮件发送。
- 角色说明区域：
  - 三个角色按钮（管理员 / 普通用户 / 只读）+ 文字说明。

#### 4.2.3 AI 聊天页（`/chat`）

参考 `aichat/原型图/src` 中的聊天界面（例如对话卡片、输入框、提示文案）：

布局：
- 左侧：会话列表（可折叠）
  - 显示「新建会话」按钮和历史会话（名称 + 最近时间）。
- 中间：对话区域
  - 顶部显示当前会话标题。
  - 中间为对话消息流，一条一条纵向排列：
    - 用户消息：右对齐，气泡背景。
    - AI 消息：左对齐，带头像/图标，支持代码块和 Markdown 渲染。
- 底部：输入区域
  - 多行文本输入框。
  - 提示文案（如支持快捷键）。
  - 发送按钮。

交互：
- 新建会话：清空当前消息，创建新的会话 ID。
- 发送消息：
  - 前端先在本地追加用户消息。
  - 调用后端 `/api/chat/stream` 或 `/api/chat`。
  - 逐步渲染 AI 的回答（本期可先用非流式，后续迭代流式）。
- 错误处理：当调用失败时，展示一条「请求失败，请稍后重试」的系统消息。

---

## 5. 后端设计（Spring Boot + H2）

### 5.1 核心实体

#### 5.1.1 用户实体 `User`

- 字段：
  - `id`：Long
  - `email`：String 唯一
  - `password`：String（BCrypt 加密存储）
  - `name`：String
  - `role`：枚举：`ADMIN` / `USER`
  - `status`：枚举：`ENABLED` / `DISABLED`
  - `lastLoginAt`：LocalDateTime
  - `createdAt` / `updatedAt`

#### 5.1.2 会话实体 `ChatSession`

- 字段：
  - `id`
  - `userId`
  - `title`：会话标题
  - `createdAt` / `updatedAt`

#### 5.1.3 消息实体 `ChatMessage`

- 字段：
  - `id`
  - `sessionId`
  - `sender`：`USER` / `AI`
  - `content`：文本内容（可为 Markdown）
  - `createdAt`

#### 5.1.4 知识文档实体 `KbDocument`（可后续实现）

- 字段：
  - `id`
  - `title`
  - `content`
  - `tags`
  - `updatedAt`

### 5.2 接口设计

#### 5.2.1 认证接口

- `POST /api/auth/login`
  - 请求：`{ email, password }`
  - 响应：
    - 成功：`{ token, user: { id, name, email, role } }`
    - 失败：返回 401 + 错误信息。
  - 登录成功时更新 `lastLoginAt`。

> token 可以先采用 JWT 或简单的 sessionId，本期不强制指定实现细节，只要求接口形式固定。

#### 5.2.2 用户管理接口（需要 ADMIN 权限）

- `GET /api/admin/users`
  - 支持分页与按邮箱/姓名搜索。
- `POST /api/admin/users`
  - 创建新用户：`{ email, name, role, status }`，返回新建用户信息。
- `PUT /api/admin/users/{id}`
  - 更新用户基本信息和角色。
- `POST /api/admin/users/{id}/reset-password`
  - 重置密码，返回新密码（或提示已重置）。

#### 5.2.3 聊天接口

- `GET /api/chat/sessions`
  - 返回当前用户的所有会话列表。
- `POST /api/chat/sessions`
  - 新建会话，可携带首条消息。
- `GET /api/chat/sessions/{id}/messages`
  - 返回指定会话的所有消息。
- `POST /api/chat/sessions/{id}/messages`
  - 发送一条用户消息，并由后端调用智谱 API 获取回复，保存一对消息记录并返回。

调用智谱 API 的逻辑：
- 在 `ChatService` 中统一封装：
  - 组装 prompt：包括用户问题 + 检索到的知识库片段（可先 mock 一段固定文本）。
  - 调用智谱 HTTP API，获取回答文本。
  - 把 AI 回答作为 `ChatMessage` 保存并返回。

---

## 6. 安全与权限控制

- 使用 Spring Security：
  - 未登录访问 `/login` 以外接口全部 401。
  - `role = ADMIN` 才能访问 `/api/admin/**`。
  - 聊天接口只能访问自己的会话与消息。
- 前端路由守卫：
  - 未登录访问任何受保护路由时跳转 `/login`。
  - 登录后访问 `/login` 自动重定向到角色对应首页。

---

## 7. 部署与环境

### 7.1 开发环境

- 后端：
  - Spring Boot 内嵌 Tomcat，端口 8080 / 8090（按需要配置）。
  - H2 数据库存储在内存或本地文件（`jdbc:h2:file:./data/aichat`）。
- 前端：
  - Vite 开发服务器，默认端口 517x。
  - 通过 Vite 代理 `/api` 到后端。

### 7.2 后续生产环境建议

- 数据库替换为 MySQL/PostgreSQL。
- 使用 Nginx 反向代理前后端与域名。
- 使用环境变量配置智谱 API Key。

---

## 8. 迭代建议

首版（MVP）重点：
- 登录 + 权限区分（管理员/普通用户）。
- 用户管理基础功能（仅管理员）。
- AI 聊天基础能力：能发问题 + 收到智谱模型回答；会话列表可切换。

后续迭代：
- 引入真正的知识检索（例如向量检索或全文检索），让回答更贴近企业文档。
- 完善 SSO/第三方登录，接入企业统一认证。
- 补充操作日志、会话导出、模型参数配置等管理功能。

