# 项目规则
## 项目定位
本工作区主要用于**创建与搭建编程项目**，以**网站项目**为主，可能包含**后端服务**。AI 协助时应
以“新站点从零搭建、前后端技术选型、目录结构、可运行示例”为重点。
## 网站项目约定
- **前端**：根据需求选择纯静态（HTML/CSS/JS）、或 SPA（React/Vue 等）；保证页面可访问、导航
与样式统一。
- **后端**：若需要接口、登录、数据持久化等，再引入后端（如 Node/Express、Python/Flask 或
FastAPI、Go 等）；前后端分离时约定好 API 形态（REST/JSON 等）。
- **结构**：建议前后端分目录（如 `frontend/`、`backend/` 或 `client/`、`server/`），或
单仓库内清晰区分；提供各自依赖说明与启动方式。
## 通用约定
- 优先写**清晰、可读、无占位符**的完整代码；必要时再考虑性能优化。
- 新项目需包含：依赖说明（如 `package.json`、`requirements.txt` 等）、简要 README、可直
接运行或构建的入口。
- 命名与结构遵循该语言/框架的常见习惯，便于后续扩展。
## 技术选型
- 未指定技术栈时，按“网站 + 是否要后端”推荐并说明理由（如：仅展示用静态站，需登录/数据用 Node
或 Python 后端等）。
- 新项目尽量**少依赖、少构建步骤**，除非用户明确需要复杂工具链。
## 回复与沟通
- 回复使用**中文**。
- 先简要说明思路或步骤，再给出具体代码或文件修改；涉及多文件时列出变更清单。

## 会话总结（每次请求结束必做）
- 每一次会话请求结束后进行会话总结，无论生成新文件还是修改已有文件都需要做总结。
- 将总结内容 **Append 追加写入**到 `README` 文件中，并注明该说明为**累积增加**。
- 总结内容应包括：
  - 会话的主要目的
  - 完成的主要任务
  - 关键决策和解决方案
  - 使用的技术栈
  - 修改了哪些文件


